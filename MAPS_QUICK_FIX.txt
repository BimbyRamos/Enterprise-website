═══════════════════════════════════════════════════════════════
  GOOGLE MAPS NOT SHOWING - QUICK FIX
═══════════════════════════════════════════════════════════════

PROBLEM:
  The map in Contact section shows an error because the Google Maps
  API key is not configured.

CURRENT STATUS:
  .env.local has: NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here
  This is a placeholder, not a real API key!

═══════════════════════════════════════════════════════════════
  SOLUTION - GET A GOOGLE MAPS API KEY
═══════════════════════════════════════════════════════════════

STEP 1: GO TO GOOGLE CLOUD CONSOLE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Visit: https://console.cloud.google.com/


STEP 2: CREATE PROJECT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. Click project dropdown (top of page)
2. Click "NEW PROJECT"
3. Name: "Networld Capital Website"
4. Click "CREATE"


STEP 3: ENABLE MAPS API
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. Go to: https://console.cloud.google.com/apis/library
2. Search: "Maps JavaScript API"
3. Click on it
4. Click "ENABLE"


STEP 4: CREATE API KEY
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. Go to: https://console.cloud.google.com/apis/credentials
2. Click "CREATE CREDENTIALS" → "API key"
3. Copy the API key (looks like: AIzaSyABC123...)


STEP 5: RESTRICT API KEY (IMPORTANT!)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. Click on the API key you just created
2. Under "Application restrictions":
   • Select: "HTTP referrers (web sites)"
   • Add referrers:
     - http://localhost:3000/*
     - https://yourwebsite.com/*
     - https://*.vercel.app/*
3. Under "API restrictions":
   • Select: "Restrict key"
   • Check: "Maps JavaScript API"
4. Click "SAVE"


STEP 6: UPDATE .ENV.LOCAL
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Open .env.local and replace:

  NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

With your actual key:

  NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=AIzaSyABC123YourActualKeyHere


STEP 7: RESTART SERVER
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Stop the server (Ctrl+C) and restart:

  npm run dev


STEP 8: REFRESH WEBSITE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. Go to: http://localhost:3000
2. Scroll to Contact section
3. Map should now load! 🎉

═══════════════════════════════════════════════════════════════
  WHAT I FIXED
═══════════════════════════════════════════════════════════════

✅ Updated Map component with better error handling
✅ Added helpful error messages
✅ Added fallback UI when API key is missing
✅ Added loading state
✅ Added console logs for debugging
✅ Created setup guide (GOOGLE_MAPS_SETUP_GUIDE.md)

═══════════════════════════════════════════════════════════════
  PRICING INFO
═══════════════════════════════════════════════════════════════

Google Maps is FREE for most websites:
  • $200 free credit per month
  • Maps JavaScript API: $7 per 1,000 loads
  • = ~28,000 FREE map loads per month

For a business website, this is usually more than enough!

═══════════════════════════════════════════════════════════════
  TROUBLESHOOTING
═══════════════════════════════════════════════════════════════

If map still doesn't load:

1. Check browser console (F12) for errors
2. Verify API key is correct (no extra spaces)
3. Make sure Maps JavaScript API is enabled
4. Check API key restrictions allow localhost
5. Clear cache and restart server

═══════════════════════════════════════════════════════════════

NEED HELP?
  See: GOOGLE_MAPS_SETUP_GUIDE.md for detailed instructions
